class Solution {
public:
    bool isPerfectSquare(int num) {
       int squareroot = sqrt(num);
       if(squareroot * squareroot == num){
           return true;
       }
       else{
           return false;
       }
    }
};
